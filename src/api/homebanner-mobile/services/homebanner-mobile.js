'use strict';

/**
 * homebanner-mobile service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::homebanner-mobile.homebanner-mobile');
